import { Hero } from "./components/Hero";

export default function MarketingHome() {
  return (
    <>
      <Hero />
      {/* Future sections (features, AI showcase, RBAC, how it works, footer CTA)
          will mount below — the design system, animation primitives, and nav
          are already in place. */}
    </>
  );
}
